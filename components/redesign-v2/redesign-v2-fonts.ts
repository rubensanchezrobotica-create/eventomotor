import { Archivo } from "next/font/google";

export const redesignV2DisplayPilot = Archivo({
  weight: "900",
  style: ["normal", "italic"],
  subsets: ["latin"],
  display: "swap",
  variable: "--font-v2-display-pilot",
});
